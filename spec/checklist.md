# Checklist

验收全项通过（2026-08-31 全量复测；以下为已验证结论摘要，契约细节见 spec.md）：

- 构建：go.mod（module github.com/JayceChant/ratchetmatch，go 1.27）；空词库/空串/非法 UTF-8/U+FFFD 关键词报错可区分；重复去重
- 语义：非重叠最左最长（含前缀取最长、断词 fail 结算、逐级弹出、无空档——必死候选不弹链 {0,000} 用例）；FindNext 以 End 迭代 == FindAll（500 组随机 + fuzz）
- 正确性：白盒自动机语义重推导（fail/outLens/byteFilter 不变量）、CSR 布局与二分分支、黑盒极端场景（自重叠/Emoji/词即整文本等）；naiveSearch 为独立 oracle，500 组随机对照一致
- 跳跃：root 态坏字符跳跃不漏报（独立 oracle 随机对照与 fuzz 覆盖）；ASCII 段字节级跳过
- FindAllOverlapping：全量保留、End 升序同 End 长度降序，与逐词 strings.Index 枚举一致
- 并发：Matcher 构建后只读，8 goroutine × 100 次 FindAll/FindNext，`go test -race` 通过
- fuzz：FuzzMatch 任意字节文本 × 关键词组合不 panic、全部不变量与 oracle 成立（3 分钟 449 万 execs 零失败）；崩溃样本回归 testdata/fuzz/
- 工程：gofmt / go vet / golangci-lint（0 issues）/ go test ./...（-race 经 Windows 侧执行）全链路通过；基准无回退（New 持平或更优；跳跃隔离对照于 2026-09-01 并入三基线体系，历史混合 ~1.97x 见 tasks.md）；三参照基线（各 100 词 × 稀疏/重叠 × 10 万 rune）：中文 Trie ~1.1–1.2x / BM ~9.6–11.0x / Index ~9.9–11.1x，混合 Trie ~1.3–1.9x / BM ~11.5–13.3x / Index ~9.4–9.7x，FindNext 首停 ~12.5–19.7x（相对逐词全文扫），等价性由 TestBaselineEquiv（两套词库 × 两份文本）锁定；2026-09-02 scan 可读性重构（Task 17）后全链路（build/vet/gofmt/fix/lint/test/-race + 基准 allocs 一致）复验通过
- CI（2026-09-01）：GitHub Actions 五工作流（ci/vulncheck/scorecard/codeql/sonarcloud，vulncheck 于 2026-09-02 自 ci.yml 拆出独立以获得单独徽章）+ Codecov 免 token 上传 + pkg.go.dev 自动抓取；README 双语徽章 8 枚（CI/CodeQL/govulncheck/codecov/SonarCloud/Scorecard/go.dev/License；「质量与 CI」结果表与文末「第三方服务说明」已于 2026-09-02 移除，README 收敛为用户视角，开通遗留项见 tasks.md 当日记录）；仅 SonarCloud 需 SONAR_TOKEN secret、Scorecard 提分需 master 分支保护。goreportcard 已于同日移除（官方宣布停止服务，其检查项由 CI 的 golangci-lint/go vet/gofmt 覆盖）。SonarCloud 覆盖率 0% 已于 2026-09-02 修复（属性名误用，见 tasks.md）；example/ 已于 2026-09-03 从覆盖率统计排除（独立 module 无测试，见 tasks.md）；修复生效需下一次 CI 扫描后到 SonarCloud 后台核对
- 示例（2026-09-03 起）：`example/` 下可独立 `go run` 的完整程序（basic/semantics/iterate/wordcount/synonyms），独立 module + replace 指向仓库根；example 模块 build/vet/gofmt/go fix/golangci-lint（0 issues）通过、逐个运行输出核对无误（Task 18，2026-09-04 增补 synonyms/ 同义词示例并给 wordcount 增补按组聚合统计——Task 25，详见 tasks.md）
- caseFold（2026-09-03，Task 19，feat/case-fold 分支）：Find 系列变参 `opts ...Option` + `WithCaseFold()`（源兼容，默认精确行为不变）；折叠自动机首次 fold 查询惰性构建（sync.Once 并发安全），词库由精确 trie 无损还原重建；rootNext/byteFilter/CSR 键轨道展开，查询热路径与精确模式同一套代码；命中区间按关键词 rune 数回退文本提取（宽度差安全），Keyword 为文本原样切片；fold 三 API 与逐位置 strings.EqualFold 枚举 oracle 300 组随机对照一致，fuzz 45s 13.6 万 execs 零失败，-race 并发惰性构建通过；全链路静态检查与测试门槛全过，精确路径基准与 allocs 无回退
- 双引擎重构（2026-09-03，Task 20）：两套私有自动机 exactMatcher/foldMatcher（独立 node 类型）共用泛型扫描引擎 machine[N]（gcshape 单态化零接口开销），对外收敛于 Matcher（exact/fold/once/foldOnly）；New 加变参支持 fold-only 模式（仅折叠自动机、查询固定 fold 且无法关闭）；fuzz 锁定 fold-only == 惰性 fold 一致性；-race/静态检查/基准复验全过（陷阱与修复记录见 tasks.md：filter 首 rune 置位、foldEngine 快路径数据竞争）
- API 定型（2026-09-03，Task 21）：**Matcher 改为密封导出接口**（isInternal），exactMatcher/foldMatcher 为两套独立实现（类型即模式，CaseFold() 判别）；模式仅由 New 选项一次性定型，Find 系列回归无变参签名（与 v0.1.0 一致）；惰性构建/trieKeywords/foldEngine 全套机制删除；双模式 = 分别 New（业界惯例：Hyperscan/aho-corasick/RE2 均以 caseless 为构建期属性，见 tasks.md 调研结论）；test/-race/fuzz/example 模块全链路通过，基准无回退
- caseFold 测试强度与性能（2026-09-03，Task 22）：语句覆盖率 100% 之外补齐两处分支级盲区——fold CSR 二分分支（段宽 35 >16，轨道展开后 root P/p 双键）与 runeStartBack 混合宽度回退（k/K/U+212A 1B↔3B、ÿ/Ÿ 1B↔2B 窄宽交错）；exact 零影响确认（-race + 覆盖率 100% + 基准与历史 allocs/时延一致）；fold 开销实测（bench_fold_test.go，TestFoldBenchEquiv 锁定对照口径）：查询与 exact 持平（含 ASCII 最坏情况 3.05 vs 3.04ms，fold 另多命中 ~1000 条），构建 1.42x→1.06x 随词库增大摊薄；README 双语已增补性能提示
- 同义词分组（2026-09-04，Task 23，feat/synonyms 分支）：`WithSynonyms(groups [][]string)` 组员自动入库（与显式关键词合并去重、同套校验；一词多组/空组/组员非法报错，fold 按归一形判冲突），`Match.Group` 恒填充分区语义（声明组按声明序 0..k-1、未声明词按去重词库序自成单元素组），`WordGroup(g)` 返回内部只读成员切片（越界 nil）、`WordGroups()` 按组号升序一次返回全部组（外层新建可重排、元素为内部只读切片；Task 24 更名并增补）；**分组为纯输出元数据，绝不改变匹配语义**（同扩充词库有/无分组 FindAll/Overlapping/FindNext 逐条对照 + 200 组随机双 oracle）；与 WithCaseFold 正交——归一形（foldKey 轨道代表，ASCII 为大写）判词身份，同形必然同组，Group 兼作规范词身份；词库可仅由组构成（New(nil, WithSynonyms(...))）；实现为节点 outGroups 平行数组（沿 fail 链共享切片、每输出 +4B），热路径仅 pendHit+group 与提交时拷贝，零新增分支；gofmt/vet/lint（0 issues）/test/-race/fuzz 20s 18.5 万 execs 全过，基准 allocs（12/10/2）与历史一致、时延噪声范围内
